var bottleColor = 'yellow';
var waterQuantity = 1;
var isFull = false;

// array
var items = ['bottle', 'mug', 'paper', 'pen'];
// items.indexOf('logens');  //-1
// items.push('envelope');
// items.push('watch');
// items.pop();

// conditionals
if (items.length >= 7) {
    console.log('you have to many');
}

else if (items.length == 4) {
    console.log('you only have four');
}

else {
    console.log('bal');
}